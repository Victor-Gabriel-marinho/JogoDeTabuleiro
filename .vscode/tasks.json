{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar e Executar",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "if exist bin rmdir /s /q bin && mkdir bin && dir /s /b *.java > sources.txt && javac -d bin @sources.txt && java -cp bin Main"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$javac"
        }
    ]
}